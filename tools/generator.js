function generator_run(obj)
{
    var text = ":" + obj.properties.length;

//    for (var i = 0; i < obj.properties.length; ++i) {
//        text += ' name=' + obj.properties[i].name;
//        text += ' value=' + obj.properties[i].value;
//    }

    write("test");
}
